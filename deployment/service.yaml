apiVersion: v1
kind: Service
metadata:
  name: bitcoin-app-service
  namespace: velero
spec:
  selector:
    app: bitcoin-app
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 8080  
  type: LoadBalancer    