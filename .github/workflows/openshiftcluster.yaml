name: Build and Deploy to OpenShift in   deployment

on:
  push:
    branches: [ main ]
    paths:
      - "bitcoin-app/**"

env:
  IMAGE_NAME: mybitcoin-app
  DOCKERHUB_USER: rukevweubio
  QUAY_USER: rukevweubio

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
 
    - name: Checkout Repository
      uses: actions/checkout@v4

 
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 18

   p
    - name: Install & Build App
      working-directory: bitcoin-app
      run: |
        npm ci
        npm run build

   e
    - name: Build Docker Image
      working-directory: bitcoin-app
      run: |
        docker build -t $IMAGE_NAME:latest .

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}


    - name: Login to Quay.io
      uses: docker/login-action@v3
      with:
        registry: quay.io
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_TOKEN }}


    - name: Tag Images
      run: |
        docker tag $IMAGE_NAME:latest $DOCKERHUB_USER/$IMAGE_NAME:latest
        docker tag $IMAGE_NAME:latest quay.io/$QUAY_USER/$IMAGE_NAME:latest

    
    - name: Push Images
      run: |
        docker push $DOCKERHUB_USER/$IMAGE_NAME:latest
        docker push quay.io/$QUAY_USER/$IMAGE_NAME:latest


    - name: Install OpenShift CLI
      run: |
        curl -LO https://mirror.openshift.com/pub/openshift-v4/clients/oc/latest/linux/oc.tar.gz
        tar -xvf oc.tar.gz
        sudo mv oc kubectl /usr/local/bin/


    - name: OpenShift Login
      run: |
        oc login ${{ secrets.OPENSHIFT_API }} \
          --token=${{ secrets.OPENSHIFT_TOKEN }} \
          --insecure-skip-tls-verify=true

 
    - name: Select Project
      run: oc project ${{ secrets.OPENSHIFT_NAMESPACE }}


    - name: Deploy to OpenShift
      run: |
        oc set image deployment/bitcoin-app bitcoin-app=quay.io/$QUAY_USER/$IMAGE_NAME:latest
